# タスク生成

## コンテキスト（参照用）
以下は全要件の一覧です。タスク生成時に環境制約やパス情報などを参照してください。

{{ all_requirements_text }}

## 対象要件（タスク生成対象）
以下の要件を満たすタスクを生成してください。

{{ requirements_text }}
{% if common_validation_text %}

## 共通品質基準
以下は全てのタスクの validation に暗黙的に含まれる必須条件です。
タスク固有の validation 基準に加えて、これらも満たす必要があります。

{{ common_validation_text }}

**重要**: タスク固有の validation にこれらを重複記載する必要はありません。
{% endif %}

## 指示
上記「対象要件」を満たすタスクを生成してください。
「コンテキスト」の情報（実行パス、環境制約など）も考慮すること。
要件間の依存関係を分析し、基盤となる部分から段階的に生成すること。

{% include 'task_generation_base.j2' %}

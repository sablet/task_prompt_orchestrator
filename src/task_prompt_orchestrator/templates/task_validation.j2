## Validation for Task: {{ task_name }}
{% if exploration_path %}

### 事前調査結果（参照用）

イテレーション開始時のコードベース調査結果が以下のファイルにあります。
**注意**: この調査はイテレーション開始時点のものです。このタスクや前のタスクで実装された部分は調査結果と異なる可能性があります。

```
{{ exploration_path }}
```

---
{% endif %}

The following instruction was executed:
{{ instruction }}

### Execution Output:
{{ instruction_output }}

### Validation Criteria:
{% for criterion in validation_criteria %}
- {{ criterion }}
{% endfor %}
{% if common_validation %}

### Common Quality Criteria (Required for ALL tasks):
{% for criterion in common_validation %}
- {{ criterion }}
{% endfor %}
{% endif %}

Please verify ALL of the above validation criteria are met{% if common_validation %}, including the common quality criteria{% endif %}.

Respond with a JSON object in the following format:
```json
{
  "approved": true/false,
  "checks": [
    {"criterion": "criterion text", "passed": true/false, "details": "explanation"}
  ],
  "summary": "overall validation summary",
  "feedback": "if not approved, specific feedback for retry"
}
```

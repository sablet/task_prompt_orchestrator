# Codebase Exploration for Verification

以下の要件を検証するために、コードベースの事前調査と必要なコマンド実行を行ってください。

## 要件ファイル
要件の定義は以下のファイルに記載されています。**まずこのファイルを読んで、検証対象の要件を把握してください。**

```
{{ all_requirements_path }}
```

## 調査・実行項目

### 1. コードベース構造
- 主要なエントリーポイント（CLI、スクリプト）の特定
- テストファイルの場所と構成
- 関連する設計ドキュメント

### 2. テスト・コマンド実行結果
検証に必要なテストやコマンドを**実際に実行**し、結果を記録してください：
- 関連するテストスイートの実行（pytest等）
- 検証対象機能を動作させるコマンドの実行
- 実行結果（成功/失敗、出力内容）

### 3. 中間生成ファイル・アーティファクト
コマンド実行により生成されたファイルの情報：
- 生成されたレポートファイルのパスと内容（該当部分）
- ログファイル
- その他の出力ファイル

## 出力形式

後続の検証エージェントが参照できるよう、以下の形式でまとめてください：

```
### コードベース構造
- CLI: <path>
- Tests: <paths>
- Docs: <paths>

### 実行結果
#### <コマンド1>
$ <実行したコマンド>
<結果の要約または重要部分>

#### <コマンド2>
...

### 生成されたアーティファクト
#### <ファイル名>
Path: <path>
Content (relevant sections):
<内容の抜粋>
```

**重要**:
- 実際にコマンドを実行してください（コードの存在確認だけでなく）
- 生成されたファイルは内容を読み取って含めてください
- 後続の検証で再実行が不要になるよう、十分な情報を含めてください
